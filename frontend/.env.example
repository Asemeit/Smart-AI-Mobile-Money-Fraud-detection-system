# Frontend Configuration
VITE_API_URL=http://localhost:5000
VITE_APP_ENV=development

# Optional: disable AI service fallback (default: true — always available)
# VITE_ENABLE_AI_SERVICE=true

# Logging
# VITE_LOG_LEVEL=info
